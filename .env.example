# Optional local overrides. The service needs no env to run — MongoDB is always
# in-memory (no connection string to configure).

# HTTP port (default 3000)
PORT=3000

# Node environment: development | production | test (default development)
NODE_ENV=development
